# 安装指引

> **先谢一句**：本插件服务于 [中国遗产地图 STELE](http://stele.geogv.org/zhcn/)——由 [@pkkj](https://github.com/pkkj) 独立维护的免费中国文保地图。
> 点位数据、坐标、名称全部来自 STELE，本插件只是补上"这里的开放时间是多少"。**请优先支持 STELE 本身。**
> 本插件为非官方第三方工具，与 STELE 官方无隶属关系。

## 前置：申请至少一个地图 API Key

### 高德地图（推荐）

1. 打开 https://lbs.amap.com/
2. 注册账号 → 实名认证（个人身份证 + 银行卡验证几分钟就过）
3. 进入 **控制台 → 应用管理 → 我的应用 → 创建新应用**
4. 应用名称随便填，应用类型选 **"其他"** 即可
5. 添加 Key：
   - 服务平台：**"Web 服务 API"**（不是 JS API / 不是 Mobile SDK）
   - 不需要勾选任何高级权限（基础配额就够个人用）
6. 拿到一串类似 `a7b1d3f8c2e5...` 的字符串，复制备用

个人开发者每日免费配额：通常 5000 ~ 30000 次/日（具体看平台规则）

### 百度地图（备份，可选）

> ⚠️ **应用类型必须选「服务端」，不是「浏览器端」。**
> 本插件调用 `place/v2/search`，属百度 **Web服务API**，只有服务端类型开放该服务。
> 选「浏览器端」会报 `status=240 APP 服务被禁用`。

1. 打开 https://lbsyun.baidu.com/
2. 注册 + 开发者认证
3. **控制台 → 应用管理 → 我的应用 → 创建应用**
4. 应用类型：**「服务端」**
5. IP 白名单填 **`0.0.0.0/0`**（插件没有固定出口 IP）
6. 服务列表里勾选 **「地点检索」**（Place API）
7. 复制 **访问应用（AK）**（别复制成「安全码（SK）」）
8. 「校验方式」保持普通校验，不要开 SN 校验

个人开发者每日免费配额：6000 次/日（需开发者认证才能享受）

| 报错 | 解法 |
|---|---|
| `status=240 APP 服务被禁用` | API Key 类型选错，或未勾选「地点检索」 |
| `status=200 应用不存在` | 把「安全码（SK）」当成 API Key 了 |
| `status=5 AK Failure` | API Key 未复制完整 |

---

## 加载扩展

### Chrome / Edge

1. 地址栏打开 `chrome://extensions`（Edge 是 `edge://extensions`）
2. 右上角打开 **"开发者模式"** 开关
3. 点击左上角 **"加载已解压的扩展程序"**
4. 选择本项目的 `src/` 目录

如果加载时报错"Manifest version 2 is deprecated" 或类似问题：
- 检查 Chrome 版本：扩展要求 Chrome 88+
- 检查 manifest.json 顶部 `"manifest_version": 3`

### 验证加载成功

- 工具栏出现红底"古"字图标 → 成功
- 点击图标弹出配置面板 → 成功
- 打开 http://stele.geogv.org/zhcn/ 在地图上点一个 POI → 详情页底部出现"开放信息"卡片 → 端到端打通

---

## 常见问题

### Q: 卡片一直显示"未配置 Key"
A: 打开插件 popup 面板，检查 Key 是否填对、是否点过"保存"。

### Q: 显示"暂未匹配到该点位的开放信息"
A: 这个 POI 在高德/百度数据库里没有被收录（多见于县区级/未定级文物）。
   卡片下方有"在高德地图中搜索 →" / "在百度地图中搜索 →"链接，点击跳转手动确认。

### Q: 显示的营业时间和景区门口对不上
A: 高德/百度的运营数据有时滞后。卡片底部已注明"信息来源于第三方地图，仅供参考"。
   重要行程请务必用景区官方电话核实。

### Q: 控制台看到 CORS 错误
A: 高德/百度 Web API 默认允许 CORS。如果遇到，请检查：
   - Key 是否在该平台的"应用"→"白名单" 中包含 `*` 或当前域
   - 高德：应用配置里 IP 白名单填 `0.0.0.0/0` 或留空
   - 百度：Referer 白名单填 `*://*`

### Q: 想在多个浏览器/电脑间共享 Key
A: 当前 Key 存在 `chrome.storage.local`，仅本浏览器有效。多设备需要分别配置。
   后续版本会考虑加 export/import 功能。
