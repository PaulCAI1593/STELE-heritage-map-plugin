# STELE 遗产地图插件

> 在 [中国遗产地图 STELE](http://stele.geogv.org/zhcn/) 的文保点位详情页，叠加一张来自**高德地图 / 百度地图**的「开放时间 · 票价 · 地址」信息卡片。
>
> 用自己申请的地图 API Key，数据只存在本地浏览器，不经任何第三方服务器。

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

---

## 🙏 致谢（请先读这段）

**这个插件能存在，全靠 [中国遗产地图 STELE](http://stele.geogv.org/zhcn/) 的作者 [Kejing Peng（@pkkj）](https://github.com/pkkj)。**

STELE 是一个由个人长期维护、**完全免费且无广告**的中国不可移动文物地图。它把全国重点文物保护单位、省级/市县级文保、世界遗产等数据整理成可检索、可点击的地图，数据质量和 UI 完成度都远超同类项目——这些整理工作本身才是这个领域最稀缺的东西。

本插件做的事情其实很轻：**它只是去问一句「这个点位在高德/百度上对应的开放时间是什么」**。真正有价值的点位数据、坐标、分类、简介、图片，全部来自 STELE。没有 STELE 这个数据底座，这个插件一行代码都没有意义。

所以：

- **请优先支持 STELE 本身**——多用它、给它反馈、向身边做文化遗产的人推荐它。
- 如果你是开发者，STELE 的作者 [@pkkj](https://github.com/pkkj) 的主页值得一看。
- 本插件是**非官方第三方工具**，与 STELE 官方无隶属关系。若作者认为有任何不妥，我会立刻配合调整或下架。

> STELE 站点地址：<http://stele.geogv.org/zhcn/>
> 作者 GitHub：<https://github.com/pkkj>

---

## ✨ 功能

- 🔍 自动识别你当前查看的文保点位（含站点内部路由切换）
- ⏰ 显示**开放时间**（优先整周描述，退化为今日）；高德没给就用百度补，反之亦然
- 🎫 显示**票价**（仅当地图给出的值确实像门票价时才显示，见下）
- 📍 显示地图上的**地址**
- 🧭 **匹配来源透明**：卡片写明"匹配到地图上的：X（直线约 N 米）"
- 🎯 **三路交叉印证**：名称、地址、坐标三路互相验证后才拍板；只有单一证据时会明确标注
- 🔗 匹配不到时给出「在高德/百度地图中搜索 →」跳转链接
- 💾 同一 POI 结果本地缓存 7 天，节省 API 配额
- 🔐 你的 Key 只存在本机 `chrome.storage.local`，不上传任何服务器
- 🚫 非文保点位不打扰：行政区、地铁站、公园等底图 POI 不会弹出卡片

## 📸 效果

在 STELE 详情页底部会出现一张与站点风格一致的小卡片：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
开放信息                          高德地图
───────────────────────────────
匹配到地图上的：北京石刻艺术博物馆（直线约 150 米）
开放时间   周二至周日 09:00-16:30
票价       15
地址       五塔寺村24号
───────────────────────────────
[ 在高德地图中搜索 → ]  [ 在百度地图中搜索 → ]

信息来源于第三方地图，仅供参考，请以现场公告为准。
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

> **关于「文物组」**：像"上海交通大学早期建筑"这类包含多个子项目的**文物组，本插件不显示表格**。
> 一个组动辄十几个子项目，其中多数在地图上并没有独立点位，硬凑一张表一半是「未匹配」、一半可能张冠李戴。
> 请直接点进具体子项目，插件会显示那个子项目自己的开放信息，
> 并自动借父项目组的地址与名称来辅助检索（详见 [DESIGN.md](DESIGN.md)）。

## 📦 安装

### 方式一：下载打包好的扩展（推荐）

1. 到 [Releases](https://github.com/PaulCAI61/STELE-heritage-map-plugin/releases) 下载最新的 `STELE-heritage-map-plugin-vX.Y.Z.zip`
2. 解压到一个**不会被删除的固定目录**（Chrome 会一直从这里加载）
3. 打开 `chrome://extensions/`，右上角开启「开发者模式」
4. 点「加载已解压的扩展程序」，选中解压出来的目录
5. 点工具栏上的插件图标，填入地图 API Key，保存

### 方式二：从源码加载

```bash
git clone https://github.com/PaulCAI61/STELE-heritage-map-plugin.git
```

然后在 `chrome://extensions/` → 「加载已解压的扩展程序」→ 选择仓库里的 **`src/`** 目录（扩展本体在 `src/`，无需构建）。

> ⚠️ 每次在 `chrome://extensions/` 里点了「重新加载」，**必须再刷新一次 STELE 页面**，否则当前标签页里跑的还是旧脚本。

## 🔑 申请地图 API Key

至少要配一个。**高德免费额度够用，5 分钟能搞定**，建议先配高德。

| 平台 | 类型要求 | 申请地址 |
| --- | --- | --- |
| 高德地图 | **Web 服务** API Key | <https://lbs.amap.com/dev/key/app> |
| 百度地图 | **服务端** AK（不是浏览器端） | <https://lbsyun.baidu.com/apiconsole/key> |

> 百度一定要选「**服务端**」类型，并把 IP 白名单填 `0.0.0.0/0`、勾选「地点检索」。
> 选成「浏览器端」会得到 `status=240 APP 服务被禁用`——这是最常见的坑。

详细图文步骤与报错对照表见 [`INSTALL.md`](INSTALL.md)。

## 🛠️ 开发

无需安装任何依赖，纯 Node 即可跑全部测试（**492 项检查**）：

```bash
npm test                 # 全部
npm run test:match       # 16 个真实文保单位：解密→抽取→名称规范化→匹配
npm run test:baidu       # 同一批真实点位走完整百度管道（跨数据源一致性）
npm run test:group       # 子项目借用父组地址 / 泛词歧义 / 大型场地
npm run test:inject      # 详情面板 DOM 注入与卡片生命周期
npm run test:switch      # 点位切换时序（迟到响应不得覆盖当前点位）
npm run test:api         # 高德/百度请求 URL 契约
npm run build            # 重新生成图标 + 打包到 release/
```

### 目录

| 路径 | 说明 |
| --- | --- |
| `src/` | **扩展本体**，直接加载这个目录 |
| `src/content/` | 内容脚本：解码、名称处理、坐标、缓存、匹配、卡片渲染 |
| `src/popup/` | 工具栏弹窗（填 Key、测试连通性、清缓存） |
| `tests/` | 492 项回归测试，`tests/fixtures/` 是 22 个真实文保的 feature 响应样本（含 16 个类型样本与 5 个改名案例） |
| `tools/` | 图标生成、打包脚本 |
| `docs/` | 实机测试说明 |
| `DESIGN.md` | **方案设计书**：坐标校正推导、三路匹配决策顺序、各类错配的成因与对策 |

### 调试入口

在 STELE 详情页打开 DevTools Console：

```js
HMP.debug.status()      // 当前路由、识别到的点位 id、已缓存点位
HMP.debug.inspect()     // 卡片/详情面板的 DOM 现场诊断
HMP.debug.fire({name:'测试', feature_type:'博物馆', geom:{coordinates:[116.4,39.9]}})

HMP.naming.extractCity(['酒泉市 敦煌市'])            // → "敦煌市"
HMP.card.plausiblePrice('81569.00')                  // → false（楼盘均价不是票价）
HMP.card.borrowedNamePenalty('兆丰花园遗址', {name:'兆丰花园', type:'商务住宅;住宅区'})
```

### 几个踩过的坑

| 现象 | 原因与对策 |
| --- | --- |
| **站点坐标整体偏移约 340 km** | 不是 WGS84↔GCJ02 那 500 m 级差异，而是仿射畸变。拟合后误差降到 ~150 m，这是"按位置找回改名点位"能成立的前提 |
| **文保名 ≠ 现用名** | 从 address/intro 挖「现为 XXX」当检索词；再配合坐标周边搜索 |
| **子项目只有名称、没有地址** | 站点 `members[]` 不含地址，但 `kid` 就是它自己的 feature id；另外 `relationinfo` 直接写着父项目组，据此借组地址 |
| **泛词子项目乱匹配**（白宫 → 外地同名地点） | 子项目改用「**父组名 + 子项名**」检索，且不搜裸名；名称命中还要卡 3 km 距离上限 |
| **小区"借用"文保名**（兆丰花园遗址 → 同名住宅小区） | 候选名是文保名前缀 + 类型是住宅 → 降权；若地址只返回该小区，让位给坐标路径 |
| **大型场地落到某个门**（圆明园 → 圆明园（东门）） | 门址/设施类在所有路径一律排除 |
| **票价显示 81569.00** | 高德 `cost` 对楼盘是房价单价（元/㎡）。只在"像门票价"时才显示 |
| **改了匹配逻辑但界面没变** | 结果是 7 天缓存。缓存键带 `DATA_VERSION`，改动后需：重载扩展 → 清除缓存 → F5 |

## 🗺️ 它到底怎么匹配的

三条路径并行跑，再交叉印证（而不是三选一）：

1. **名称检索** —— 文保名 / 挖出的现用名 + 市级行政区
2. **地址检索** —— 行政区 + 门牌号（关键字会逐级放宽）
3. **坐标周边** —— 仿射校正后的坐标做 1 km 周边搜索

决策顺序优先采信"互相印证"的结果；**只被坐标单独支持的候选会被拒绝**（那通常是隔壁商户）。
完整推导、权重取值与实测数据见 [`DESIGN.md`](DESIGN.md)。

## ⚖️ 免责与合规

- 本插件是**非官方第三方工具**，与 STELE 官方及高德/百度均无隶属关系。
- 插件在你的浏览器里读取 STELE 页面**公开展示的数据**，并按你配置的 Key 去请求高德/百度，
  不搭建任何中转服务器，不收集、不上传任何用户数据。
- 开放时间等一切信息均来自第三方地图，**可能滞后或出错，出行前请以现场公告为准**。
- 仓库中**不包含** STELE 站点的任何源码或样式文件；`tests/fixtures/` 仅为测试所需的最小数据样本。
- 若 STELE 作者 [@pkkj](https://github.com/pkkj) 对上述用法有任何异议，我会立即调整或下架。

## 🤝 参与

Issue / PR 都欢迎。提交匹配错误时，**带上卡片上的「匹配到地图上的：X（直线约 N 米）」那一行**，
再附上点位名称——这一行基本能定位到底是名称、地址还是坐标路径出的问题。

## 📝 更新日志

见 [CHANGELOG.md](CHANGELOG.md)。

## 📄 许可证

[MIT](LICENSE) © 2026 PaulCAI61
