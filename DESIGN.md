# 遗产地图插件 — 方案设计书

> **项目代号**：`heritage-map-plugin`
> **目标站点**：`http://stele.geogv.org/zhcn/`
> **核心能力**：在每个文物保护单位点位的详情页底部叠加"开放信息"卡片，数据来自用户自配的高德地图 / 百度地图 API。

---

## 1. 形态定稿

| 决策点 | 选项 |
| --- | --- |
| 平台 | Chrome / Edge 浏览器扩展（Manifest V3） |
| 注入范围 | 仅在 `stele.geogv.org/zhcn/*` 的 `/geo/:id`、`/detail/:id`、`/info/:id` 路由下激活 |
| 展示位置 | 详情面板底部追加一张卡片 |
| 数据来源 | 高德地图（默认）+ 百度地图（备份），用户可在 popup 切换 |
| **匹配策略** | **以文保单位名称 + 行政区为主关键字**；坐标仅用于排序辅助 |
| **文物组处理** | `feature_type === "文物组"` 时不查自身；遍历 `members` 对每个子项目独立查询，渲染成表格 |
| 关键字拼接 | `"adminLast" + " " + name`（如 `"敦煌市 莫高窟"`），避开不同城市重名混淆 |
| 降级处理 | 未匹配时，给出"在百度/高德地图中搜索"两条跳转链接 |
| 缓存策略 | `chrome.storage.local` 缓存 `(provider, name, admin)` → 结果，TTL 7 天 |

---

## 2. 用户流程

### 2.1 单 POI（博物馆/风景区/5A景区等）

```
打开 stele.geogv.org/zhcn/ → 点击地图上的 POI
   ↓
content.js 劫持 window.fetch，从 /api/v1/feature/{id} 解密得到 POI 数据
   ↓
isGroup 判断为 false → 走 processSingle
   ↓
抽取：文保名 / 市级行政区 / 现用名(address·intro) / 站点坐标
   ↓
坐标仿射校正 stele→WGS84（误差~150m）→ GCJ02 / BD09
   ↓
查 chrome.storage.local 缓存 → 命中直接渲染
   ↓ 未命中
┌─ 路径 A：名称检索 ────────────────────────────┐
│  关键词 = "市级 + 检索名"（现用名优先）        │
│  高德 v5/place/text   百度 place/v2/search     │
│  matcher.pick 取最相似候选                     │
│  命中且 sim ≥ 0.85 → 采用（matchType=name）    │
└───────────────────────────────────────────────┘
   ↓ 未达标
┌─ 路径 B：位置检索（改名兜底）─────────────────┐
│  高德 v5/place/around  location=GCJ02 radius=1km│
│  百度 place/v2/search  location=BD09 radius=1km │
│  候选打分 = 类型(像不像文物景点)×0.55           │
│           + 距离×0.35 + 名称相似×0.10           │
│  采用最优点（matchType=location）               │
└───────────────────────────────────────────────┘
   ↓
在详情面板底部插入「开放信息」卡片
（位置匹配时明确标注现用名与直线距离）
```

### 2.2 文物组（如莫高窟） —— ⚠️ 当前不显示卡片

**本版起，文物组不显示任何卡片。** 点进具体子项目时，才显示那个子项目自己的开放信息。

原因：一个组动辄十几个子项目，其中多数在地图上没有独立点位，只能靠
"项目组地址 + 子项目名"推测，错配率高（总办公厅 → 上海商学院(徐汇校区)）。
一张一半「未匹配」、一半可能出错的表格，价值低于它带来的误导。

下面的实现细节（`memberSearchNames`、按 `kid` 拉子项目自身地址、独立文保才允许坐标兜底、
地址关键字逐级放宽）**都保留在 `processGroup` 里，但未接入入口**（见 `processFeature`），
由 `group_tests` 覆盖，将来若要重新启用可直接接回。

```
打开 stele.geogv.org/zhcn/ → 点击"莫高窟"聚合点
   ↓
content.js 劫持 fetch，解密后 isGroup 判断为 true → 走 processGroup
   ↓
卡片渲染 loading："正在查询 N 个子项目的开放信息…"
   ↓
遍历 members（限制前 10 个防配额爆），每个子项目独立走 processSingle 流程
   ↓
并发查询 → 收集结果
   ↓
卡片渲染为表格：
  子项目      | 今日营业    | 来源
  莫高窟      | 08:30-17:30 | 高德地图
  西千佛洞   | —           | 未匹配（附跳转链接）
  ...
```

---

## 3. 关键技术细节

### 3.1 三道匹配路径（组合检索，不切换）

插件用**三条互补路径**同时定位地图上的 POI，再交叉印证，而不是"三选一"：

| 路径 | 依据 | 擅长 | 短板 |
|---|---|---|---|
| **A. 名称检索** | 文保名 / 现用名 + 市级行政区 | 名称一致时精确、便宜 | 文保名与现用名无关时失效 |
| **B. 地址检索** | 行政区 + 门牌号（如`上海市 徐汇区 漕溪北路336号`） | 门牌号不随改名变化 | 旧门牌、地址已重编时失效 |
| **C. 坐标周边** | 校正后坐标做周边搜索 | 名称完全无关也能找到 | 怕"隔壁商户"，精度依赖坐标校正 |

**为什么组合而不是让用户切换主策略**：三路的失效原因互不重叠（名称怕改名、地址怕旧门牌、坐标怕隔壁商户），
而实测中每一个错匹配（`LANN丨蘭·泰式古法按摩`、`…草坪与建筑(打卡点)`、`马勒别墅(东门)`）都是**只有坐标支持**的。
组合 + 交叉印证把这类错误挡掉；切换主策略反而会把正确证据一起丢掉。
`strategyMode`（`combine` 默认 / `name` / `address` / `geo`）只保留给排查问题用，不是常规设置。

决策顺序（每个数据源内）：
1. 名称强命中（`sim ≥ 0.85`）**且与地址命中的是同一个 POI** → 采用，标注"地址印证"（最可信）
2. 名称强命中 → 采用
3. 地址命中与坐标命中是同一个 POI → 采用，标注"坐标印证"
4. 地址命中 → 采用（门牌号比坐标更稳）
5. 仅坐标命中，且类型确实像文物/景点 → 采用；否则视为"只被坐标支持的隔壁商户"，宁可不匹配
6. 退回较弱的名称为结果

其中"类型像文物/景点"包含了文保建筑**被征用后的现用身份**：学校（圣约翰大学→华东政法大学）、
宗教场所、机关办公（天主教大修院→徐汇区人民政府）等，都算合法结果，按实写出来即可。
只有**设施类**（公交站/地铁站/停车场/大门/厕所/打卡点）被排除。

#### 3.1.0 谁该显示开放信息：按"保护身份"，不按点位类型

遗产地图的底图上除了文保单位，还铺了一层**普通 POI**（行政区、地铁站、公园、行政村、国宾馆…）。
点开它们只想看"这是什么"，不需要开放信息，插件也不应该介入。

站点用 `category` 字段表达保护身份：

| 点位 | `feature_type` | `category` | 是否显示 |
|---|---|---|---|
| 息焉堂 | `教堂` | `[上海市文物保护单位, 上海市优秀历史建筑]` | ✅ |
| 马迭尔宾馆 | `宾馆酒店` | `[全国重点文物保护单位]` | ✅ |
| 西王小区 | `居民区` | `[上海市优秀历史建筑]` | ✅ |
| 西郊乡 / 大厦村 / 新天地公园 / 地铁站 | `乡级行政区` 等 | `[]` | ❌ |

⚠️ **不能按 `feature_type` 判断**：`马迭尔宾馆` 的类型是"宾馆酒店"、`息焉堂` 是"教堂"，
两者的 `feature_type` 都不像文物，但都是货真价实的国保/市保单位；反过来，"公园""寺庙"
里也混着大量非文保的普通 POI。**类型描述的是"它现在是什么"，`category` 才描述"它受什么保护"。**

判定规则（`isDesignated`）：
- `category` 非空数组 → 有保护身份，正常查询
- `category` 是空数组 → 普通 POI，不介入，并收掉上一个点位的卡片
- `category` 字段整个缺失 → 按"有保护身份"处理（**失败开放**）：
  万一站点改了字段名，宁可多显示一点，也不要让插件整体失效

#### 3.1.0.1 匹配来源一律写明

卡片必须让用户能核对自己看到的是哪个地图 POI。旧实现只在"名称对不上"时才提示，
于是 `息焉堂` 这种**地图名与文保名一模一样**的点位什么也不写——用户无从判断这份
开放时间来自哪里，更看不出它可能匹配到了外地同名点位。现在三种情况都写明，并带上距离：

| 情况 | 文案 |
|---|---|
| 名称不同 | `匹配到地图上的：X（直线约 N 米）` |
| 名称相同 | `地图上同名点位：X（直线约 N 米）` |
| 靠位置找回 | `该点位现用名与文保名称不同，已按位置匹配到：X（直线约 N 米）` |

距离由 `matcher.pick` 给出（目标坐标与候选同系，GCJ02/BD09 已对齐），
名称路径也把 `dist` 一并返回——**同名点位却在几公里外，就是匹配错了**，这是最直观的判据。

#### 3.1.0.2 子项目与"借用项目组地址"
文物组的子项目（`members`）在站点数据里**只有名称和坐标，没有地址**。
子项目名往往是"图书馆""总办公厅"这类泛词，光靠名称查不到、光靠坐标会落到隔壁单位
（实测：`总办公厅` 匹配到了 `上海商学院(徐汇校区)`）。
因此子项目按以下顺序检索：

1. 名称：`项目组名 + 子项目名`（如`上海交通大学早期建筑 总办公厅`），再退化到裸名
2. 地址：**借用项目组的地址**（如`上海市 徐汇区 华山路1954号`）→ 落到真正所在的校区/院落
3. **不做坐标兜底**（`noGeoFallback`），否则每栋楼都会被套上外圈大学/景区的开放时间

第 2 步是"名称查不到时退到外层的正确载体"，比落到无关的隔壁单位诚实得多；
卡片上会以 `↳ 实际匹配到的名称` 注明，不冒充子项目本身的开放时间。

**成员自己的 `kid` 就是它的 feature id。** 站点返回的 `members[]` 只有 `kid/name/geom`，
但按 `kid` 拉一次 `/api/v1/feature/{kid}` 就能拿到**子项目自己的地址和简介**：

| 子项目 | 按 kid 拉回来的地址 | 简介里的现用名 |
|---|---|---|
| 徐家汇观象台旧址（徐家汇组） | `蒲西路166号` | 上海气象博物馆 |
| 中院 / 图书馆（交大组） | 无（纯子项） | 无 |

于是子项目按"自己的地址 → 组地址"的顺序取用，名称检索词也带上自己的现用名
（并保证现用名排进 `MAX_NAME_VARIANTS` 的前三位，别被截掉）：

1. 名称：`组名 + 子项名` → 子项裸名 → **子项自己的现用名** → `组名 + 现用名`
2. 地址：**子项目自己的地址** → 没有才用组地址
3. **不做坐标兜底**（`noGeoFallback`）

成员数据按 `kid` 在内存里缓存，点开不同子项目不会重复拉取。

#### 3.1.0.2.1 「正在浏览哪一组」

站点在文物组面板里点开子项目时，会把地址栏切到**子项目自己的 id**。
若照单全收，就会从"整组表格"掉成"单个子项目的卡片"——正是用户反馈的
"项目组表格经常不显示，只看到子项目的开放信息"。

因此 `processGroup` 会记住当前组（`memberGroup = {feature, kids}`）：
随后派发上来的点位若其 `kid` 属于这一组，就继续渲染整组表格，
并把该行加 `.hmp-focus` 高亮。派发别的点位即解除该状态。

配套的坑在 content.js：那里的幂等判据必须是**"最近一次派发给 card.js 的 id"**，
**不能**用"派发过的 id 集合"。用户会 A → B → A 来回切（项目组 ↔ 子项目就是这种），
用集合的话，切回 A 会被当成"早就派发过"而跳过，卡片永久停在 B 上。

##### 子项目允许坐标兜底的条件

子项目用 `noGeoFallback` 决定跑不跑坐标路径，判据是**它自己有没有独立保护身份**
（按 `kid` 拉回来的 feature 的 `category`）：

| 子项目 | 自己的 category | 坐标兜底 |
|---|---|---|
| 徐家汇观象台旧址 / 徐家汇藏书楼旧址… | 市保 + 优秀历史建筑 | ✅ 允许（它们在地图上各有自己的点位） |
| 交大的「中院」「图书馆」「总办公厅」 | 无 | ❌ 禁用（否则每栋楼都套上校区时间） |

这一条正是"表格里永远未匹配"的解药：徐家汇那一组 7 个子项全是独立文保单位，
单独点开能匹配到、表格里却匹配不到，差别就在坐标路径开不开。

#### 3.1.0.3 子单元降权：三种形态

地图 POI 里混着大量"本体的一部分"，它们往往比本体更近、更容易赢，必须降权（`subUnitPenalty`，×0.6）。
难点在于**写法不统一**，实测已遇到三种：

| 形态 | 例子 | 判据 |
|---|---|---|
| ① 本体 + **连接号** + 后缀 | `上海国际饭店-会议中心` | 原始全名是前缀，且紧跟连接号 |
| ② 连接号前段比命中变体更长 | `上海孙中山故居纪念馆-草坪与建筑` | 头段 ≈ 变体 + 若干字 |
| ③ 本体 + **附属设施后缀**（无连接号） | `上海音乐厅咖啡厅` | 本体名是前缀，后缀命中附属设施词表 |

形态 ③ 只能按后缀**词表**判（没有连接号可依赖），所以词表必须窄——只收"楼里的配套设施"：

> 咖啡厅 / 茶室 / 餐厅 / 商店 / 礼品店 / 售票处 / 服务中心 / 管理处 / 办公区 / 停车场 / 洗手间 …

⚠️ **绝不能对后缀一刀切**：以下都是正确匹配，必须放行——

> `故宫` → `故宫博物院`、`良渚遗址` → `良渚遗址公园`、
> `上海孙中山故居` → `上海孙中山故居纪念馆`（纪念馆是其正式名）、
> `马勒住宅` → `上海马勒别墅饭店`（改名后的现用身份，连前缀都对不上）

形态 ① 必须拿**原始全名**比较，不能用 `searchNames` 拆出的短变体：
`马当路45-47号住宅` 会被拆出变体 `马当路45`，拿变体去比会把**精确同名**的候选
误判成"本体 + -47号住宅"而错误降权。

#### 3.1.1 ⚠️ 坐标实测与校正（关键突破）

用 16 个不同类型文保单位实测，STELE 站点坐标相对真实位置发生**仿射畸变**
（既有平移，也有约 1.5% 的缩放），不是 WGS84↔GCJ02↔BD09 那种 500 m 级的差异：

| 点位 | 站点坐标 | 真实坐标(约) | 未校正偏差 |
|---|---|---|---|
| 故宫 | 113.131, 41.510 | 116.397, 39.918 | 327 km |
| 布达拉宫 | 87.455, 31.130 | 91.117, 29.657 | 387 km |
| 丽江古城 | 96.720, 28.312 | 100.234, 26.872 | 382 km |
| （16 个全部同向） | | | 323–387 km |

对这个变换做**最小二乘仿射拟合**：

```
lon_real = 0.98431778 × lon_stele + 5.03246552
lat_real = 0.98833425 × lat_stele − 1.11021542
```

效果（见 `tests/geo_tests.js`）：

| | 平均误差 | 最大误差 |
|---|---|---|
| 不校正 | **347 km** | 387 km |
| 校正后（拟合点） | **1.03 km** | 2.99 km |
| 校正后（**留出验证**，5 个未参与拟合的改名点位） | **0.15 km** | 0.49 km |

留出验证的 5 个点：

| 文保名称 | 校正后坐标 | 实际坐标 | 误差 |
|---|---|---|---|
| 真觉寺金刚宝座 | 116.3229, 39.9434 | 116.3283, 39.9420 | 0.49 km |
| 觉生寺 | 116.3307, 39.9670 | 116.3306, 39.9663 | 0.07 km |
| 智化寺 | 116.4247, 39.9159 | 116.4250, 39.9160 | 0.03 km |
| 先农坛 | 116.3848, 39.8754 | 116.3845, 39.8756 | 0.03 km |
| 保国寺 | 121.5175, 29.9811 | 121.5170, 29.9820 | 0.11 km |

**~150 m 的精度足以支撑 1 km 半径的周边搜索** —— 这正是解决改名问题的钥匙。

#### 3.1.1.1 三家坐标系如何对齐

仿射校正是「站点 → 真实 WGS84」，**与地图厂商无关**；再按各家要求转换：

| 使用方 | 坐标系 | 转换链 | 参考点 |
|---|---|---|---|
| 本站（校正后） | WGS84 | `steleToWgs84()` | — |
| **高德** | GCJ02 | WGS84 → GCJ02 | `poi.gcj` |
| **百度** | BD09 | WGS84 → GCJ02 → BD09 | `poi.bd09` |

精度实测（5 个留出点位，见 `tests/geo_tests.js`）：

| 目标坐标系 | 平均误差 | 最大误差 |
|---|---|---|
| → WGS84 | 0.145 km | 0.486 km |
| → GCJ02（高德） | 0.146 km | 0.487 km |
| → BD09（百度） | 0.146 km | 0.489 km |

**结论：百度与高德精度一致**（差 < 1 m）。因为 GCJ02→BD09 是一对平滑的近刚体
变换，误差按 1:1 传递，不会放大。往返换算误差也 < 1 m
（`gcj02ToBd09` ↔ `bd09ToGcj02`、`wgs84ToGcj02` ↔ `gcj02ToWgs84`）。

**两处百度特有的坑（已规避）**：

1. **不传 `ret_coordtype`**
   若传 `ret_coordtype=gcj02ll` 期望百度把结果转成 GCJ02，一旦百度未按预期转换，
   返回仍是 BD09，与 GCJ02 参考点混算会让**所有距离整体偏 ~890 m**——
   在 1 km 半径的周边搜索里足以把正确目标挤出半径，属于致命的**静默**错误。
   现在的做法：让百度返回**原生 BD09**，并用 `poi.bd09` 作参考点，两端同系、自洽。

2. **名称检索路径不得带 `location`/`radius`**
   百度一旦带上就切换成「圆形区域检索」。而站点**原始**坐标偏 340 km，
   会把搜索锁死在错误位置 → 恒 0 结果。只有校正后的坐标才用于周边搜索。

> 高德侧对应地：`location`/`radius` 属于 `place/around` 语义，
> 名称检索用 `place/text` 且不带坐标。

#### 3.1.2 改名问题：文保名 ≠ 现用名

这是纯名称匹配**无解**的一类：

| 文保名称 | 现用名 | 名称关系 |
|---|---|---|
| 真觉寺金刚宝座 | 北京石刻艺术博物馆 | 完全无关 |
| 觉生寺 | 大钟寺古钟博物馆 | 部分相关（又名大钟寺） |
| 先农坛 | 北京古代建筑博物馆 | 完全无关 |
| 智化寺 | 北京文博交流馆 | 完全无关 |
| 良渚遗址-莫角山遗址 | 良渚古城遗址公园 | 部分相关 |

两条解法**同时使用**：

**解法 1 — 从站点数据挖现用名**（`naming.extractModernNames`）
站点数据里常藏着线索，覆盖率约 40%：
```
address: "五塔寺村24号北京石刻艺术博物馆内"    → 北京石刻艺术博物馆
address: "北三环西路甲31号，现为大钟寺古钟博物馆" → 大钟寺古钟博物馆
intro  : "觉生寺又名大钟寺"                    → 大钟寺
```
挖出的现用名**排在最前面**参与名称检索。

**解法 2 — 校正坐标 + 周边搜索**（`card.searchNearby`）—— 兜底且通用
```
站点坐标 ──仿射校正──▶ WGS84 (误差~150m)
   ├─ WGS84→GCJ02 ──▶ 高德 v5/place/around  (radius=1000m)
   └─ WGS84→BD09  ──▶ 百度 place/v2/search  (圆形区域检索)
   ↓
候选按 [类型像不像文物景点] + [距离] 打分：
   类型分 2=博物馆/公园/寺庙/遗址…  1=未知  0=餐饮/商场/停车… （0 直接丢弃）
   综合分 = 类型分×0.55 + 距离分×0.35 + 名称相似度×0.10
```
这样即使名称完全对不上，也能靠"位置相同"找回正确的 POI。

**UI 上如何让用户放心**：位置匹配的结果会在卡片里明确标注
「该点位现用名与文保名称不同，已按位置匹配到：北京石刻艺术博物馆（直线约 440 米）」，
而不是假装是名称匹配。

#### 3.1.3 名称规范化与变体

```js
// naming.js —— 行政区/名称规范化
extractCity(["江苏省南京市玄武区、雨花台区"])  // → "南京市"
extractCity(["酒泉市 敦煌市"])                  // → "敦煌市"（取最精确一级）

searchNames("承德避暑山庄及其周围寺庙")
// → ["承德避暑山庄及其周围寺庙", "承德避暑山庄"]
searchNames("皖南古村落－西递、宏村")
// → ["皖南古村落－西递、宏村", "皖南古村落", "皖南古村落－西递"]
searchNames("鼓浪屿近代建筑群")
// → ["鼓浪屿近代建筑群", "鼓浪屿"]

// matcher.js —— 对「目标变体 × 候选变体」笛卡尔积取最高分
nameScore(a, b) = max over (pairScore(ax, by))

// pairScore 的三条防误匹配规则：
//   A. 规范化把名字截成"短桩"时不可信
//      （否则"良渚遗址"和"良渚博物院"会双双变成"良渚"而撞车）
//   B. 候选 = 目标 + 设施后缀（博物馆/游客中心…）时降权 0.85
//      （避免"承德避暑山庄博物馆"盖过"避暑山庄"）
//   C. 候选是目标的简短常用名时加权 +0.06
//      （"承德避暑山庄" ⊃ "避暑山庄"）

// 通过阈值：名称为主，不再卡距离
function passes(sim, dist) {
  if (sim >= 0.7) return true;                              // 名称为主
  if (sim >= 0.55 && Number.isFinite(dist) && dist <= 500) return true;
  return false;
}
```

**实测（16 个文保单位全链路）**：

| 站点名称 | 抽出市级 | 命中 | sim |
|---|---|---|---|
| 故宫 | 北京市 | 故宫博物院 | 0.850 |
| 布达拉宫 | 拉萨市 | 布达拉宫 | 1.000 |
| 颐和园升平署 | 北京市 | 颐和园 | 0.810 |
| 大足石刻 | 重庆市 | 大足石刻 | 1.000 |
| 平遥古城 | 晋中市 | 平遥古城 | 1.000 |
| 龙门石窟 | 洛阳市 | 龙门石窟 | 1.000 |
| 秦始皇陵 | 西安市 | 秦始皇陵 | 1.000 |
| 明孝陵 | 南京市 | 明孝陵 | 1.000 |
| 承德避暑山庄及其周围寺庙 | 承德市 | 避暑山庄 | 0.893 |
| 曲阜孔庙及孔府 | 曲阜市 | 曲阜孔庙 | 1.000 |
| 丽江古城 | 丽江市 | 丽江古城 | 1.000 |
| 皖南古村落－西递、宏村 | 黄山市 | 皖南古村落 | 1.000 |
| 云冈石窟 | 大同市 | 云冈石窟 | 1.000 |
| 殷墟 | 安阳市 | 殷墟 | 1.000 |
| 良渚遗址-莫角山遗址 | 杭州市 | 未匹配（地图无同名 POI，走降级链接） | — |
| 鼓浪屿近代建筑群 | 厦门市 | 鼓浪屿 | 1.000 |

### 3.2 关键字拼接（避开重名）

名称检索使用 `"市级" + " " + 名称` 形式作为关键字（**不用原始的 `admin` 多级串**，
因为实测 admin 格式不统一：`"北京市 东城区"`、`"江苏省南京市玄武区、雨花台区"`）：
- 高德：`keywords=敦煌市 莫高窟&region=敦煌市`
- 百度：`query=南京市 明孝陵&region=南京市`

好处：避免"平遥古城 / 丽江古城"这类同名不同城市的混淆。
`region` 只传**市级单一名称**，否则 API 会判为非法或忽略。

**多变体检索**：按 `searchTerms`（现用名优先，再是文保名变体）逐个尝试，
命中 `sim >= 0.95` 即提前结束；最多 3 个变体，兼顾准确率与 API 配额。

⚠️ **名称检索路径不传 `location`/`radius`**：百度一旦带上 `location`+`radius`
就变成圆形区域检索；而站点**原始**坐标偏 340 km，会把搜索锁死在错误位置。
只有经过 `geo.steleToWgs84()` 校正后的坐标才会用于周边搜索（路径 B）。

### 3.3 文物组流程

`isGroup(feature)` 判断条件：
- `feature.feature_type === "文物组"`（明确标记），或
- `Array.isArray(feature.members) && feature.members.length > 0`（兜底）

`processGroup(feature)`：
- 渲染 loading 状态
- 限制 `GROUP_MEMBER_LIMIT = 10`，避免配额爆
- 并发调用 `queryOnePoi(cfg, subPoi)` 对每个 member
- 渲染表格：子项目名 / 今日营业 / 来源（高德/百度/未匹配）

子项目查询参数：
- `name` = member.name
- `adminLast` = 整个文物组的 adminLast（继承自聚合）
- `keywords` = `"敦煌市 西千佛洞"`（拼接）

### 3.4 fetch 劫持

```js
const TARGET_PATTERNS = [
  /\/api\/v1\/feature\/([^/?]+)/,
  /\/api\/v1\/query\/([^/?]+)/,
  /\/api\/v1\/query\?xy=/
];

window.fetch = async function (input, init) {
  const response = await origFetch(input, init);
  if (isTargetUrl(url)) {
    response.clone().text().then(text => {
      const decoded = tryDecode(text); // 站点 feature 端点是混淆字符串
      if (decoded && decoded.name) {
        window.dispatchEvent(new CustomEvent('hmp:poi-loaded', { detail: decoded }));
      }
    });
  }
  return response;
};
```

### 3.5 b.so 解密算法

`decoder.js` 直接移植自 `app.js` 中的 `b.so` 类（循环减法密码），每条响应自带密钥。

### 3.6 DOM 注入与 React 重渲染

`MutationObserver` 监听 `.modal.show .modal-body` 出现，React 重渲染时自动重新注入卡片。

### 3.7 Chrome 扩展权限

```json
{
  "permissions": ["storage"],
  "host_permissions": [
    "*://stele.geogv.org/*",
    "https://restapi.amap.com/*",
    "https://api.map.baidu.com/*"
  ]
}
```

> 用户 Key 存 `chrome.storage.local`，仅用户本人可见。

### 3.8 卡片样式

- 继承站点主色调 `#EC7063`（文保单位主色）
- 单 POI 卡片：`poi-section-content-padding hmp-card` 类复用站点布局
- 文物组卡片：`hmp-card-group` 额外类，渲染为表格

---

## 4. 项目目录结构

```
heritage-map-plugin/
├── README.md                项目说明 / 快速上手
├── DESIGN.md                本文件
├── INSTALL.md               详细安装指引 + FAQ
├── package.json             开发期测试脚本（npm test）
├── .gitignore
├── src/                     按此目录加载扩展（无需构建）
│   ├── manifest.json        MV3 manifest（host_permissions 需覆盖两个地图域名）
│   ├── content/
│   │   ├── decoder.js       b.so 解密算法
│   │   ├── naming.js        行政区/名称规范化（市级抽取 + 名称变体 + 现用名提取）
│   │   ├── geo.js           坐标仿射校正 + WGS84→GCJ02→BD09
│   │   ├── cache.js         chrome.storage.local 缓存（键含数据版本号）
│   │   ├── http.js          统一请求入口：经 service worker 转发（MV3 跨域）
│   │   ├── matcher.js       名称变体匹配 + 防误匹配规则
│   │   ├── card.js          三路检索 + 印证分级 + 卡片 DOM 注入
│   │   ├── content.js       路由识别与派发（URL 兜底 + 时序防抖）
│   │   ├── content.css      卡片补充样式（主体复用站点样式类）
│   │   └── apis/
│   │       ├── amap.js      高德 v5 place/text + place/around
│   │       └── baidu.js     百度 place/v2/search（行政区 + 圆形区域）
│   ├── popup/               Key 配置面板（测试前自动保存）
│   ├── background/          MV3 service worker（缓存清理 + 跨域请求代理）
│   ├── icons/               PNG 图标（16/32/48/128，由 tools/make-icons.js 生成）
│   └── LICENSE
├── tests/                   回归测试（npm test，共 558 项检查）
│   ├── fixtures/            22 个真实点位的 feature 响应 + picks.json
│   ├── run_tests.js         16 个文保单位匹配测试
│   ├── baidu_parity_tests.js 百度侧全链路 + 跨数据源决策一致性矩阵
│   ├── geo_tests.js         坐标校正精度（含留出验证）+ 坐标系转换
│   ├── renamed_tests.js     改名场景端到端（真觉寺→北京石刻艺术博物馆）
│   ├── group_tests.js       子项目借用父组、泛词歧义、大型场地多门牌号
│   ├── combine_tests.js     三路组合、数据源选择与字段互补
│   ├── quality_tests.js     匹配质量（设施类排除、可能不开放提醒）
│   ├── api_tests.js         高德/百度请求 URL 契约 + 跨域架构守护
│   ├── fields_tests.js      字段映射与容错（含百度坐标四种形态）
│   ├── inject_tests.js      DOM 注入与卡片生命周期
│   ├── switch_tests.js      点位切换时序（迟到响应不得覆盖当前点位）
│   ├── regression_tests.js  防误匹配回归 + 端到端
│   └── smoke.js             按 manifest 顺序加载全部脚本
├── tools/                   图标生成、打包脚本
├── docs/                    实机测试说明（打包时复制进 release/）
└── research/                调研产物（不参与加载；STELE 自身文件已 gitignore）
    └── FEASIBILITY.md
```