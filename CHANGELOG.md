# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.10] — 2026-09-23

### 修复 — 名字相近但「不求连续」的那一类，此前完全看不见

上海马桥遗址匹配不到 220 米外的「马桥古文化遗址公园」。原因在相似度**算法**：

| 算法 | 「上海马桥遗址」vs「马桥古文化遗址公园」 |
|---|---|
| levenshtein（按最长串做分母） | **0.222** ← 把长度差重罚 |
| 最长公共**子串** | 0.333 ← 只看得见连续的「马桥」 |
| **最长公共子序列** | **0.667** ← 「马桥」+「遗址」按顺序都在 |

两个名字按顺序共用「马桥」和「遗址」，中间隔着"古文化"——这明显是相近的名字，
而前两种算法都看不见。

新增 `lcsComponent()`：分母用**完整文保名**，问的是"文保名有多少个字按顺序
出现在 POI 名里"。`passes()` 本来就有"相似度 ≥0.55 且距离 ≤500m"的放宽，
220 米的「马桥古文化遗址公园」因此进得了候选集。

> 早先的做法是把"遗址/公园"这类词从名字里 normalize 掉。
> 但**「遗址」恰恰是这类点位的核心词**，删掉反而丢了信息。
> 现在不删了，改成按子序列比对。

### 三处护栏（都是实测／回归逼出来的）

1. **只用完整原名算，不能用截断变体。**
   `良渚遗址-莫角山遗址` 会拆出变体「良渚遗址」，
   而「良渚古城遗址公园」正好把它整段包住 → 满分 → 把精确同名挤掉。
   （第一次改就栽在这里，是既有的 `regression_tests` 拦下来的。）
2. **上限 0.8。** "两段词对上了"足以让候选进候选集，
   但不足以单独构成"名称强命中"（`NAME_STRONG_SIM = 0.85`）。
3. **候选以全名开头时不加。** 那是"本体 + 后缀"
   （`大足石刻宝顶山景区`、`承德避暑山庄博物馆`），归设施/子单元规则管。

### 实测结果（真实接口，五个基准点位 · 两家）

| 点位 | 百度 | 高德 |
|---|---|---|
| 上海马桥遗址 | 马桥古文化遗址公园 | **马桥古文化遗址公园** |
| 崧泽遗址 | 崧泽古文化遗址 | 崧泽古文化遗址 |
| 钟和公寓 | 钟和公寓 | 钟和公寓 |
| 商船会馆 | 商船会馆 | 商船会馆 |
| 董家渡天主堂 | 董家渡天主堂 | 董家渡天主堂(暂停开放) |

### 测试

558 项（新增：子序列进候选集、子项目不得因共用组名匹配到父级、
LCS 上限、"本体+后缀"不走 LCS、设施后缀降权仍生效）。

## [0.3.9] — 2026-09-23

### 修复 — 括号里的内容只是"位置"，不是"身份"

钟和公寓匹配到了 **無名咖啡馆(钟和公寓店)**，与万达广场(上海马桥店)同一型。
0.3.7 只是让这类候选"不吃包含加成"，但还留着 0.68 的底子；
现在按"括号里写的是位置，不是身份"直接**降权**（`PAREN_BRANCH_PENALTY = 0.6`），
并且放在 `pairScore` 最后**独立生效**，无论前面走了哪条分支都要再降一档：

| 候选 | 改前 | 改后 |
|---|---|---|
| 無名咖啡馆(钟和公寓店) | 0.667 | **0.400** |
| LAWSON罗森(钟和公寓店) | 0.633 | **0.380** |
| 万达广场(上海马桥店) | 0.682 | **0.409** |
| 凯迪拉克·上海音乐厅 | 0.900 | 0.900（不受影响） |
| 凯迪拉克·上海音乐厅(南门) | 0.900 | 0.900（目标名在括号**外**） |

前三个都掉到 0.7 门槛以下，连"弱命中"都算不上。

### 实测结果（真实接口，逐条）

| 点位 | 高德 | 百度 |
|---|---|---|
| 钟和公寓 | ✅ 钟和公寓 | ❌ 绍兴公园 |
| 崧泽遗址 | ✅ 崧泽古文化遗址 | ✅ 青浦区崧泽遗址博物馆 |
| 上海马桥遗址 | ❌ 崇明区马桥村党群服务站 | ✅ 马桥古文化遗址公园 |

### ⚠ 仍未解决：兜底路径太宽松

上表两个 ❌ 都是**兜底**造成的，不是名称打分：

- 百度·钟和公寓 → 坐标兜底捞到 **绍兴公园**
- 高德·上海马桥遗址 → 地址兜底捞到 **崇明区马桥村党群服务站**
  （崇明离马桥 40 多公里，说明这个地址命中的相似度极低却仍被采纳）

这已经是第四次同类问题了（前三次：马桥/万达、崧泽/村委会、以及本次）。
根因是**决策链的优先级是逐条打补丁长出来的**，没有统一过：
"仅地址""仅坐标"这些兜底各自有各自的准入条件，谁在前面谁说了算。
逐条修已经修到第四次，说明该把这套优先级重构成一张**可解释的评分表**了
（所有单路证据放在一起按分数取最高，而不是按路径顺序短路）。
这是结构性改动、影响面大，**需要确认后再做**，本版未动。

### 测试

553 项（既有守卫覆盖：万达广场、括号在目标名之外不受影响等）。



## [0.3.8] — 2026-09-22

### 修复 — 类型正确的「弱名称命中」不该输给「名字毫不相干的地址命中」

崧泽遗址匹配到了**崧泽村村委会**。日志里三路各自找到了什么：

```
名称=崧泽古文化遗址(0.57,弱)   类型=风景名胜;风景名胜   ← 类型正确，名字也相关
地址=崧泽村村委会              相似度 0.33              ← 只是"同址的另一个单位"
坐标=崧见餐厅                  类型不像文物，不采用
```

名称路径命中 `崧泽古文化遗址`（相似度 0.571，不够"强命中"的 0.85），
于是走到"仅地址"兜底，被 `崧泽村村委会` 顶掉。

地址检索是在一段**地址串**上找 POI，同一个地址上有村委会、消防队、居委会、商铺……
名字可以毫不相干。而 0.3.7 只是把「**坐标**命中且像文物」提到了弱地址之前，
**名称**路径的弱命中的没管——它一直排在最后。

现在加一条：名称命中即使不够强，只要**类型确实像文物/景点**（`heritageRank === 2`），
就优先于弱地址。仍然排除远处同名（`nameTooFar`）、借用文保名的住宅、
以及「本体-子单元」形态，所以"白宫 → 华东政法大学"那类改名场景不受影响。

### 实测结果（真实接口）

```
[baidu] 崧泽古文化遗址  (仅名称（sim 0.57，类型像文物景点，未经地址/坐标印证）)
[amap]  崧泽古文化遗址  (仅名称（sim 0.57，类型像文物景点，未经地址/坐标印证）)
```

你接受的两个答案之一。顺带记录一个**没动**的现象：

高德给 `上海崧泽遗址博物馆` 带了 `parent="B001508164"`，于是吃到
`structuralPenalty` 的 ×0.7，分数从 0.980 掉到 0.686，
输给 `崧泽古文化遗址` 的 0.790。也就是说**名字最像、且是真正可参观实体的那个**
反而被"它是某个 POI 的子项"这条规则压下去了。

这条规则本身是为商场里的小店之类设计的，用在"遗址里的博物馆"上并不合适。
但这次的两个候选你都接受，而改动它会影响所有带 `parent` 的候选
（范围不明），所以**这一版没有动**，先记下来。

### 测试

553 项（新增：类型像文物的弱名称胜出而非村委会、远处同名仍被 `nameTooFar` 挡住）。


## [0.3.7] — 2026-09-22

### 修复 — 包含加成的误伤（0.3.4 引入，我自己写的）

上海马桥遗址匹配到了附近的**万达广场**。根因是 0.3.4 加的
「候选名整段包含目标名 → 相似度下限 0.9」被用歪了：

```
目标全名「上海马桥遗址」
  → nameVariants 拆出短变体「上海马桥」
  → 撞上「万达广场(上海马桥店)」：候选把变体整段包住 → 抬到 0.90 → "名称强命中"
  → 而正确的「马桥古文化遗址公园」相似度不足 0.7，被 passes 拒掉
```

区别在于：`凯迪拉克·上海音乐厅` 是「本体挂冠名」，
`万达广场(上海马桥店)` 是「**位于此地的另一家店**」——后者的目标名落在**括号里**。
现在目标名出现在候选的括号中时不吃包含加成（`0.682 < 0.7`，连门槛都过不了）。

### 修复 — 「名字对不上的地址命中」不该压过「坐标就在原地的文物」

地址检索是在一段**地址串**上找 POI，同一个地址上有消防队、居委会、商铺……
名字可以毫不相干。实测：上海马桥遗址 →

```
地址命中「闵行区马桥镇专职消防队」  相似度 0.182  直线 1090 m   ← 被采纳
坐标命中「马桥古文化遗址公园」       直线  218 m                ← 明明更对
```

原有的「仅地址」兜底排在坐标兜底之前，于是后者根本没机会。
现在顺序是：强地址（名字也对得上）→ 坐标且类型像文物景点 → 弱地址。

### 实测结果（真实接口）

```
[baidu] 马桥古文化遗址公园  (仅坐标（类型像文物景点，未经名称/地址印证）)
        时间=09:00-16:00
```

### 已知问题（尚未修）

**高德侧这一例可能给不出结果**。高德的坐标检索里，`同仁别墅3号`（274 m）
与 `马桥古文化遗址公园`（218 m）都在 1 km 内，而 `pick` 只返回**一个**候选；
选中的那个类型不像文物就被否掉，正确的那条随之丢失。
这一版没有动坐标路径的候选取舍（取一个 vs 取多个），
因为缺少足够样本判断改成"取多个"会不会在别处引入误配。

### 测试

550 项（新增：括号里的分店限定不吃包含加成、冠名式包含仍吃加成、
括号在目标名之外时不受影响）。


## [0.3.6] — 2026-09-22

### 修复 — 反查到的本体没能顶掉已选中的子单元（我自己上一版引入的）

0.3.5 加了 `parent_id` 反查，但商船会馆在百度侧**仍然**匹配到
862 m 外的「上海救火联合会旧址」。日志里能看到反查明明成功了：

```
baidu ✅ 由子项的 parent_id 反查到本体「商船会馆」（sim 1.00）
baidu 三路结果：名称=商船会馆-音乐剧《耋戏生》(1.00,弱) ...   ← 却还是子单元
>> [baidu] 上海救火联合会旧址   (仅坐标…)
```

原因：`nameScore` 会把**候选名按连接号拆成变体**。
`商船会馆-音乐剧《耋戏生》` 拆出 `商船会馆`，与目标逐字相同 → 子单元的相似度**也是 1.00**。
而我写的是严格比较：

```js
if (!best || m.sim > best.sim) best = m;   // 两边都是 1.00 → 永远换不掉
```

于是反查白做，最终落到坐标兜底。现在反查出来的**本体**无条件优先于
任何「本体-子单元」形态（`!best || best.isSub || m.sim > best.sim`）。

### 实测验证（真实接口）

```
>> [baidu] 商船会馆   (仅名称（sim 1.00，未经地址/坐标印证）)
     时间=10:00-16:00  地址=上海市黄浦区会馆街38号
>> [amap] 商船会馆
```

两家都出 `商船会馆`，百度给出了 `10:00-16:00`。
真实的 `parent_id` 反查结果：

```
name="商船会馆"  tag="旅游景点;文物古迹"
classified="旅游景点;人文景观;文物古迹"  hours="10:00-16:00"
```

### 测试

546 项（新增回归守卫：名称路径选中子单元时，反查到的本体必须顶掉它——
两个候选的 sim 都是 1.00，正是这个 bug 的复现条件）。


## [0.3.5] — 2026-09-22

### 新增 — 由子项的 `parent_id` 反查本体（百度）

百度**不会在名称检索里返回"本体"**。实测（真实接口）：

```
搜「上海音乐厅」→ 只返回
  凯迪拉克·上海音乐厅-正门        parent_id=fd007c5e2d33266da9f729a8
  凯迪拉克·上海音乐厅-东门        parent_id=fd007c5e2d33266da9f729a8
  凯迪拉克·上海音乐厅-地下停车场   parent_id=fd007c5e2d33266da9f729a8
本体「凯迪拉克·上海音乐厅」本身**不出现**。
```

而这些子项会被设施类规则丢掉（正门 / 停车场），于是名称路径一无所获，
最后落到坐标兜底误配到 451 m 外的另一个馆。

现在会在设施类过滤**之前**收集 `parent_id`，名称路径失败时走
`place/v2/detail` 反查，拿到的就是本体：

```
name=凯迪拉克·上海音乐厅  tag=休闲娱乐;剧院  shop_hours=09:00-20:00
```

只有"父 POI 名称与本点位相符"时才采用（避免拿别处的父 POI 张冠李戴）。
高德没有这套结构，不会因此多发请求。

`callBaidu` 顺带学会了详情接口的**单数** `result` 形状（搜索接口是 `results`）。

### 结论 — 百度的开放平台不提供关闭状态

你提到"百度地图里董家渡天主堂能搜到暂停开放的标签"。逐字段查完了，
**不在百度 Place API 里**：

- `place/v2/search`（`scope=2`）——`detail_info` 全部键：
  `classified_poi_tag, tag, navi_location, type, detail_url, overall_rating,
   comment_num, shop_hours, label, children, regular_open_hour`
- `place/v2/detail`（按 uid 取详情）——多给了
  `scope_type, brand, price, telephone, service_rating, environment_rating, town`，
  **仍然没有**任何关闭状态字段
- 把这份详情 JSON **每个键值都跑了一遍关闭词匹配**，零命中
- 接口自带的 `detail_url`（HTML 页）→ `302`，无内容
- `place/v3/search` → **404，v3 不存在**（此前 `place/v3/detail` 返回的
  `302 天配额超限` 是通用跳转，不代表该接口存在）

所以那个标签只存在于百度地图网页/App 的**内部接口**里，开放平台不返回。
关闭状态目前只能靠高德——而高德确实是有的，只是写在**名称**里：
`董家渡天主堂(暂停开放)`。0.3.3 的"状态探针"正是去取这一条。

### 实测结果（真实接口）

| 点位 | 首选 | 结果 |
|---|---|---|
| 董家渡天主堂 | 百度 | `董家渡天主堂` ✓ 关闭提示经探针取自高德「暂停开放」 |
| 董家渡天主堂 | 高德 | `董家渡天主堂(暂停开放)` ✓ |
| 上海音乐厅 | 高德 | `凯迪拉克·上海音乐厅` ✓ |
| 上海音乐厅 | 百度 | 本体经 `parent_id` 反查可得 ✓（配额耗尽前已验证接口本身） |

### ⚠ 配额

测试期间把百度那条 Key 的**当日配额用完了**（`status=302 天配额超限`），
所以 `parent_id` 反查没能在当天跑完整个端到端流程——
接口本身已直接验证，包在扩展里的接线由测试 ㉒ 用真实响应形状覆盖。

### 测试

544 项（新增：`parent_id` 反查本体、父 POI 名称不符时不采用、
高德路径不误调百度详情接口）。


## [0.3.4] — 2026-09-22

这一版全部来自**真实接口实测**，不是构造数据。所用点位：
上海董家渡天主堂、上海音乐厅、商船会馆。

### 修复 — 百度侧"类型"字段一直是错的

百度的 `detail_info.type` **不是品类，是检索范围**。真实返回长这样：

```json
{"type":"scope", "tag":"旅游景点;教堂",
 "classified_poi_tag":"旅游景点;教堂;天主教堂"}
{"type":"life",  "tag":"交通设施;充电站"}
```

而代码取的是 `detail.type` —— 于是**百度侧每个候选的类型都是 `"scope"`**，
所有按类型做的判断（设施类排除、可参观类型加分）在百度侧等于**全部失效**。
实测后果：上海音乐厅在百度侧匹配到了
「星星充电充电站(上海音乐厅充电站)」。

现在品类按 `classified_poi_tag` → `tag` → `label` → `type` 的顺序取，
并跳过 `scope` / `life` 这类检索范围值与「暂停营业」这类状态标签。

### 修复 — 纯门牌号型的 POI 被当成了匹配结果

地址检索会稳定返回一类"名字就是路名 + 门牌号"的条目：

| 文保 | 百度地址检索返回 |
|---|---|
| 商船会馆 | `会馆街38号` |
| 上海音乐厅 | `延安东路523号` |

它们没有开放时间，地址还常是「黄浦区」这种残缺值。采纳了等于给用户一张**空卡片**——
比"未匹配"更糟，因为看起来像查到了。现在这类名字直接按设施类排除。

### 修复 — 包含关系拿不到应得的相似度

`pairScore` 里只处理了"候选是目标的简短常用名"（`承德避暑山庄` ⊃ `避暑山庄`），
**反过来的方向没处理**：候选名把目标名整段包住时（`凯迪拉克·上海音乐厅` ⊃ `上海音乐厅`）
只有 0.750。实测后果：上海音乐厅在本体与远房同名之间选错——

```
"凯迪拉克·上海音乐厅"  0.750   ← 本体，被压过
"上海音乐谷"          0.800   ← 3.5 km 外的另一处
```

现在这种情况相似度下限为 0.9。**但这一支必须排在"本体 + 设施后缀"降权之后**：
两者都命中时要降权，不能被包含加成抬成 1.0
（`承德避暑山庄博物馆` 一度从 0.85 被抬到 1.00，压过了本该选中的「避暑山庄」，
是既有的 `regression_tests` 拦下来的）。

### 修复 — 剧院/音乐厅/剧场不算"可参观的文化场所"

`HERITAGE_TYPE_RE` 里有博物馆、美术馆、教堂、学校、政府机关，**唯独没有剧院**。
而 STELE 把它标成 `剧院` 的文保（如上海音乐厅）正需要这一档，
否则在类型加分上输给类型泛化的候选（`体育休闲服务场所`）。

### 实测结果（真实接口）

| 点位 | 首选 | 结果 |
|---|---|---|
| 董家渡天主堂 | 百度 | `董家渡天主堂` ✓ 关闭提示经探针取自高德「暂停开放」 |
| 董家渡天主堂 | 高德 | `董家渡天主堂(暂停开放)` ✓ 名称自带状态 |
| 上海音乐厅 | 高德 | `凯迪拉克·上海音乐厅` ✓（此前错选 `欧森演出.上海音乐厅`） |
| 上海音乐厅 | 百度 | `上海博物馆埃及展` ✗ —— 见下方已知问题 |
| 商船会馆 | 高德 | `商船会馆` ✓ |

### 已知问题（实测发现，尚未修）

- **上海音乐厅在百度侧无法得到正确结果**。百度 Place API 里**根本没有**
  「上海音乐厅」这个 POI（只有 `凯迪拉克·上海音乐厅-正门` / `-地下停车场` 等子项）。
  名称路径只找到 3.5 km 外的「上海音乐谷」（被 `nameTooFar` 挡掉），
  地址路径只找到 `延安东路523号`（现在被排除），最后落到坐标兜底，
  采纳了 451 m 外的「上海博物馆埃及展」（类型确实像文物景点）。
  这是百度的数据缺口，但坐标兜底的门槛是否需要收紧（当前只卡类型 + 1 km），
  需要更多实测样本才能定，**这一版没有动它**。

### 测试

539 项（新增：真实取值下的百度品类、纯门牌号排除、包含关系下限，
以及"设施后缀降权不被包含加成盖掉"的守卫）。


## [0.3.3] — 2026-09-22

### 修复

- **状态标签根本没送到判定逻辑手里**（真正的根因，线索点位：上海董家渡天主堂）

  `mapPoi` 一直有在认真取 `tag`：

  ```js
  tag: detail.tag || p.tag || '',        // 百度
  tag: pickFrom(sources, ['tag']),       // 高德
  ```

  但 `extract()` 是**手写字面量**重建的对象，没有把 `tag` 抄进去——
  而 matcher 和卡片用的都是 `extract` 的结果。于是 `closureHint(info)` 里的
  `info.tag` **永远是 `undefined`**：0.3.1 加的 tag 扫描，一行都没生效过。

  实测日志里能同时看到这两件事，非常直白：

  ```
  [HMP] 百度状态类字段：tag=暂停开放          ← 字段确实在接口返回里
  [HMP] baidu 状态探针：未发现关闭类标记       ← 却没送到判定手里
  ```

  教训记在测试里了：单元测试直接构造 `{name, tag}` 喂给 `closureHint` 是**能过**的。
  "字段被映射出来"和"字段被交出去"是两件事，测试必须**穿过 `extract`**。
  现在两家都有这样的接线测试（组合测试 ⑳）。

- **另一家的关闭标记此前拿不到**（新增「状态探针」）

  首选那家给了正常营业时间时，`supplementFields` 不会再问另一家——
  于是"暂停开放"若恰好在另一家身上就永远看不到。这正是实测场景：
  高德返回 `08:00-11:00,13:00-17:00`，而百度标着「暂停开放」。

  现在会在这种情况下补一次**名称检索**（1 次请求，不是三路各一次）
  专门看另一家的状态字段；只有确认是同一处才采用，卡片上写明是**哪一家**标的，
  不笼统说成"地图信息显示"。

- **状态写在「开放时间」字段里时也不提示**

  暂停营业的点位，两家的营业时间字段里写的直接就是「暂停开放」：
  高德 `business.opentime_today` / `_week`，百度 `detail_info.shop_hours`。
  关闭判定此前只扫 `name` / `status` / `tag`，**偏偏漏了开放时间本身**——
  卡片会把这个串原样当开放时间显示出来（"开放时间：暂停开放"），却一句提醒都不给。

  现在这两类字段也纳入判定。同理，很短的 `description`（≤30 字，常是状态说明）
  也算，长段落不算。

- **`samePlace` 判定为否时，此前仍会把字段补上去**

  该函数的设计前提写得很清楚：「只有确认是同一处，才能把 A 的开放时间贴到 B 上」。
  但代码在判定为否时**只打了日志、没有 return**，于是"不补充"的日志之下照补不误。
  这条分支极难触发（能通过名称门槛的候选彼此多半也像），所以一直没暴露。
  `git log` 里当时也记了"几乎不可达"，正因为测不到，才没人发现它少了一句 return。

### 排查

- 高德侧的字段诊断与百度侧对称：两家都会把**值**看起来像状态的字段打印一次
  （`[HMP] 高德状态类字段：…` / `[HMP] 百度状态类字段：…`）。
  之前只有百度侧有，且无从确认"暂停开放"究竟由哪家、放在哪个字段里。

### 测试

530 项（新增：状态标签**穿过 `extract`** 的接线、状态探针的四种情形、
状态写在 `opentimeToday` / `opentimeWeek` 时的识别与不误报、
卡片上真的渲染出警告条、`samePlace` 判定为否后必须 return）。


## [0.3.2] — 2026-09-22

### 修复

- **「本体-子单元」不再冒充本体**（实测：商船会馆 → 商船会馆-音乐剧《耋戏生》）

  这与 0.3.0 修掉的"借用文保名的住宅"是**同一个毛病**：子单元降权
  （`subUnitPenalty`）只参与**排序**，而"名称强命中"用的是**原始相似度**。
  子单元名包含本体名，相似度天然 ≈ 1.0，于是"命中子单元"直接拍板，
  轮不到坐标/地址那一路去找真正的本体。

  现在子单元命中和借用名一样**不能作强命中**，必须由地址或坐标印证；
  并且印证成立时，采纳的是**非子单元那一路**的名义
  （名称与坐标都落在子单元、只有地址找到本体时，采用地址找到的本体）。
  实在只有子单元一条记录时仍会给结果，但如实标成
  「仅名称（命中为「本体-子单元」，可信度最低）」，不会假装可信。

- **短 `description` 里的状态说明也纳入"可能不开放"判定**

  很短的 `description`（≤30 字）常是状态说明（"暂停营业"）；
  长段落是介绍文字，里面出现"修缮/拆除"多半是历史叙述，**不算**关闭信号——
  否则几乎每个文保建筑的简介都会触发误报。

### 排查

- 两家返回的 POI 里，凡**值**看起来像状态的字段都会被打印一次
  （`[HMP] 百度状态类字段：…`）。之前怀疑状态藏在别处却无从确认，
  这条日志能一次定位它到底叫哪个字段。
- 选中「本体-子单元」时打印**候选次序**（相似度/得分/是否子单元/距离），
  用来回答"本体到底在不在候选里、排第几、为什么输"。

### 测试

514 项（新增：子单元与本体同时出现、名称一路只有子单元、
名称与坐标同为子单元而地址找到本体、只剩子单元时的兜底与标注、
精确同名不受牵连、短/长 description 的关闭判定）。


## [0.3.1] — 2026-09-22

### 修复

- **百度地图的「暂停营业 / 装修中」等状态标签现在会给出提示**
  百度把这类信号放在 `detail_info.tag` 里，而关闭判定此前只查
  `name` 与 `status` 两个位置，于是百度的状态标签完全没有提示。
  现在两家的 tag 都单独取出（高德对应 `business.tag`），并纳入关闭判定。
  顺带修掉一个副作用：`tag` 此前被当作 `type` 的兜底值，
  「暂停营业」这种状态串会被喂给类型判定 —— 现在状态类 tag 不再冒充类型，
  品类类的 tag 仍可兜底。

### 测试

502 项（新增：两家 tag 字段的取出与兜底、closureHint 对 tag 的识别与不误报）。


## [0.3.0] — 2026-09-22

首个适合公开使用的版本。相较 0.2.0，主要是**修掉一整类"百度不工作"的问题**，
并把决策逻辑重构成可验证的形态。

### 修复 — 百度数据源

此前"切到百度却仍是高德结果"由**四个独立问题**叠加造成，逐个定位并修复：

- **跨域请求改经 service worker 转发**
  Chrome 从 MV3 起移除了内容脚本绕过 CORS 的能力（`host_permissions` 不再豁免）。
  高德接口带 `Access-Control-Allow-Origin` 所以直连能通，**百度接口不带 → 请求被浏览器拦下**。
  而「测试 Key」在弹窗（扩展页面）里能成功，**测试通路与实际通路不同**，长期掩盖了这一点。
  新增 `content/http.js` 作为统一请求入口，经 background service worker 转发。
- **三条检索路径不再吞掉请求异常**
  此前请求失败被静默记为"未搜到候选"，把真实原因藏了起来。现在会如实抛出，
  卡片显示「名称检索失败：…」而不是「未搜到候选」。
- **地址检索补上 `region`**
  百度要求 `region` / `location+radius` / `bounds` 三者必居其一，全空即
  `status=2 Parameter Invalid`。旧代码按高德习惯传了空 region（高德的 city 只是可选过滤器）。
- **请求的城市只写在 `region` 里**
  旧实现把城市同时拼进 `query` 和 `region`（城市重复），较长的文保名会搜不到。
- **百度坐标解析改为容错模式**
  旧写法要求坐标是 number，百度若返回字符串就得到 `null` → 坐标路径全废（distance 为 NaN
  被全部跳过）、名称路径在改名场景也过不了（dist 为 Infinity）。现覆盖四种形态：
  对象、字符串、`"lat,lng"` 字符串、以及坐标放在 `detail_info` 里。

### 修复 — 匹配决策

- **决策改为按印证强度分级，单一证据不再直接拍板**
  「名称强命中」曾是独立充分的证据，但"名字像"恰恰是最容易被骗的一路
  （同名外地点位、同景区子单元、以及借用文保名的住宅小区）。
  现在：三重印证（名称=地址=坐标）> 两路印证 > 单一证据。
- **借用文保名的住宅不得作强命中**
  `兆丰花园` ⊂ `兆丰花园遗址`，相似度 1.0，会直接命中"名称强命中"，
  轮不到"地址+坐标双重印证"给出的正确答案（中山公园）。
- **补上「旧址 / 故居 / 旧居 / 原址」类后缀变体**
  `searchNames('四行仓库抗战旧址')` 此前只返回它自己。地图 POI 通常不带这个后缀
  （实际叫「上海四行仓库抗战纪念馆」），于是只剩全名去搜 ——
  高德靠模糊匹配勉强能中，百度直接返回空。
- **住宅小区"借用"文保名时降权**（前缀 + 住宅类型）
- **大型场地多门牌号**：门牌号只作同级候选的决胜依据，不压过类型；
  门址/设施类在所有路径一律排除（不再落到「圆明园（东门）」）
- **票价合理性过滤**：地图的 cost 字段对楼盘是房价单价（元/㎡），
  不再显示「票价 81569.00」这类荒谬值

### 新增 — 两家数据源互补

- **字段级互补**：首选那家没给开放时间时，用另一家补上（卡片注明来源）。
  有开放时间时不额外请求，避免翻倍消耗配额。
  另有安全阀：确认两家指向同一处（名称相似或落点在文保本体附近）才互补。
- **数据源选择修正**：修掉"选了百度但没填百度 Key"时产出 `["amap","amap"]`
  的重复查询；兜底时在卡片上说明原因
- **缓存键改用实际作答的数据源**：避免兜底结果被写进首选键，
  导致用户补好 Key 后 7 天内仍读到旧的兜底结果

### 修复 — 其他

- **「在百度地图中搜索」改用官方 URI API**（`api.map.baidu.com/place/search`），
  补齐必选参数 `output=html` 与 `src`；并**带上校正后的坐标定中心** ——
  否则百度拿不到中心点，地图会停在默认位置（表现为"点进去坐标在海里"）
- **弹窗「测试 Key」先自动保存再测试**，并提示未保存的改动
  （此前"测试通过"不代表插件用上了）
- **文物组不再显示表格**：子项目多无独立地图点位，容易张冠李戴；
  改为点进子项目时借用父项目组的地址与名称检索（父组取自 `feature.relationinfo`）

### 新增 — 测试

**492 项零依赖回归测试**（纯 Node 运行）。本次新增的重点覆盖：

- `baidu_parity_tests.js`：16 个真实文保单位走**完整百度管道**，
  外加**跨数据源决策一致性矩阵** —— 同一批候选以两家格式喂入，断言结论一致。
  这条矩阵第一次运行就抓到了"借用文保名"那个 bug。
- 跨域架构守护断言：两个接口都必须经 service worker，且不再直接 `fetch`
- 百度坐标四种形态、URL 契约、字段映射、双向互补
- 这些测试有实际约束力：把坐标解析改回旧写法后，16 项百度测试**全部失败**

### 文档

- 重写 `docs/实机测试说明.md`（此前历次编辑导致内容重复三遍且损坏）
- `DESIGN.md` 补充：坐标仿射校正推导、三路印证分级、子单元降权的三种形态、
  各项错配的成因与对策
- `README.md` 按实际功能重写，并把**致谢 STELE 作者**放在首节

---

## [0.2.0]

- 打包输出目录改为 `release/`
- 文物组不再显示开放信息表格

## [0.1.0]

- 首个可用版本：识别点位、三路检索、卡片注入
